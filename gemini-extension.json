{
  "name": "crawl-extension",
  "version": "1.0.0",
  "description": "A tool for crawling websites and generating local documentation.",
  "main": "tools/crawl_urls.js",
  "tools": [
    {
      "name": "crawl_urls",
      "description": "Crawls a list of URLs and saves them as Markdown files in an output directory.",
      "parameters": {
        "type": "object",
        "properties": {
          "urls": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "A list of URLs to crawl."
          },
          "output_dir": {
            "type": "string",
            "description": "The directory where Markdown files will be saved."
          },
          "template": {
            "type": "string",
            "description": "The extraction template to use (e.g., 'technical-docs', 'api-reference', 'minimal').",
            "enum": ["technical-docs", "api-reference", "minimal"],
            "default": "technical-docs"
          }
        },
        "required": ["urls", "output_dir"]
      }
    }
  ]
}
